# Infrastructure

Local infrastructure starts with Docker Compose at the repository root.

Planned additions:

- Object storage for local image upload testing.
- Search engine once PostgreSQL full-text search is no longer enough.
- Reverse proxy configuration for production deployment.
- CI configuration.
